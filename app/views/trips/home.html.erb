<%= render "trips/navbar" %>

<h1 class="top-title">hello, <%= @logged_in_user.name %>! </i></h1>

<!-- Trips cards display -->
<div class="card-container">
  <div class="card-deck justify-content-center">
    <% @logged_in_user.trips.each do |trip| %>
      <a href="/trip/index/<%=trip.id%>/<%=trip.start_date%>" method="get">
        <div class="card mb-3 border-right rounded-right d-flex" style="border-radius: 2rem 0">
          <div class="card-body justify-content-center align-self-center">
            <h5 class="card-title"><%= trip.destination %></h5>
            <div class="inside-card">
              <p class="card-text align-items-center justify-content-center">
                <%= trip.start_date.strftime("%d %b %y") %>
              </p>
            </div>
            <br>
          </div>
        </div>
      </a>
    <% end %>
    <div class="card mb-3 border-right rounded-right d-flex new-trip-modal" style="border-radius: 2rem 0" data-toggle="modal" href="" data-target="#newTripModal">
      <div class="card-body justify-content-center align-self-center">
        <h5 class="card-title add-more-trips">+</h5>
        <div class="inside-card">
          <p class="card-text align-items-center justify-content-center add-more-trips">add more trips</p>
        </div>
        <br>
      </div>
    </div>
  </div>
</div>
<!-- end of Trips cards display -->

<footer class="bottom-space"></footer>

<!-- end of homepage -->

<%= render "trips/new-trip" %>